server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    location / {
      return 444;
    }

    location ~ /.well-known {
      root /var/www/letsencrypt;
      allow all;
    }

}
